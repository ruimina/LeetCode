给定一个string，查找最长每个字符都不重复的字串。
查找字串可以通过两个下标i，j去滑窗，不重复可以用set或map实现。
之前想到的是对i，j两次循环，每个字符依次加入map中，重复则退出。
复杂度O(n^2)，忽略了i++之后，j不必回到i+1，而是可以在当前位置继续滑窗。
这样改进后复杂度为O(2n)，但是利用map中的value记录下标，可以进一步改进。
循环j，找到重复元素后，i可以直接跳到重复元素后的元素，复杂度为O(n)。
PS:使用map时，insert一个已有键值会失败，map[key]=value已有键值，会更新value。